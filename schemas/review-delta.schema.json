{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Delta code review (rounds 2+)",
  "type": "object",
  "additionalProperties": false,
  "required": ["verdict", "summary", "resolved_findings", "new_findings", "regressions", "affected_acceptance_criteria", "confidence"],
  "properties": {
    "verdict": {"enum": ["pass", "changes_required", "blocked"]},
    "summary": {"type": "string"},
    "resolved_findings": {
      "type": "array",
      "items": {"type": "string", "pattern": "^F-[0-9]+$"},
      "$comment": "TEMPORARY: 'uniqueItems: true' removed to satisfy OpenAI structured-output response_format API (rejects uniqueItems as of 2026-08). Restore or replace with alternative uniqueness enforcement once the API supports it or a client-side validator layer is added."
    },
    "new_findings": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "severity", "category", "file", "line_start", "description", "evidence", "recommended_fix"],
        "properties": {
          "id": {"type": "string", "pattern": "^F-[0-9]+$"},
          "severity": {"enum": ["critical", "high", "medium", "low"]},
          "category": {"enum": ["correctness", "security", "reliability", "performance", "maintainability", "testing", "compatibility", "documentation"]},
          "file": {"type": ["string", "null"]},
          "line_start": {"type": ["integer", "null"], "minimum": 1},
          "description": {"type": "string"},
          "evidence": {"type": "string"},
          "recommended_fix": {"type": "string"}
        }
      }
    },
    "regressions": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "severity", "category", "file", "line_start", "description", "evidence", "recommended_fix"],
        "properties": {
          "id": {"type": "string", "pattern": "^F-[0-9]+$"},
          "severity": {"enum": ["critical", "high", "medium", "low"]},
          "category": {"enum": ["correctness", "security", "reliability", "performance", "maintainability", "testing", "compatibility", "documentation"]},
          "file": {"type": ["string", "null"]},
          "line_start": {"type": ["integer", "null"], "minimum": 1},
          "description": {"type": "string"},
          "evidence": {"type": "string"},
          "recommended_fix": {"type": "string"}
        }
      }
    },
    "affected_acceptance_criteria": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "status", "evidence"],
        "properties": {
          "id": {"type": "string"},
          "status": {"enum": ["satisfied", "partially_satisfied", "not_satisfied", "not_verifiable"]},
          "evidence": {"type": "string"}
        }
      }
    },
    "confidence": {"type": "number", "minimum": 0, "maximum": 1}
  }
}
